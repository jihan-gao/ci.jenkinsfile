pipeline{
    agent any
    
    stages{
        stage("build"){
	    steps{
	        script{
                mvnHome = tool "M3"
                sh "sh${mvnHome}/bin/mvn -version"
		}
	    }
	}
    }
}
