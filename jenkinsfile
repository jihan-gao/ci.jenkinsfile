pipeline{
    agent any
    
    stages{
        stage("build"){
	    steps{
	        script{
                mvnHome = tool "M3"
                sh "${mvnHome}/bin/mvn -version"
		}
	    }
	}
    }
}
